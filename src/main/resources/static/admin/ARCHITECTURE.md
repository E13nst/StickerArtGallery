# Архитектура админ-панели

## 📁 Структура проекта

```
admin/
├── index.html                  # Управление пользователями
├── stickers.html               # Управление стикерсетами
├── generation-logs.html        # Логи генерации
├── stars-packages.html         # Управление Stars Packages
├── art-rules.html              # Управление ART Rules
├── prompt-enhancers.html       # Управление Prompt Enhancers
├── style-presets.html          # Управление Style Presets
├── login.html                  # Страница авторизации
├── css/
│   └── admin.css               # Кастомные стили
└── js/
    ├── api.js                  # Централизованный API client
    ├── auth.js                 # Логика авторизации
    ├── utils.js                # Утилиты (форматирование, уведомления)
    ├── table.js                # Переиспользуемый компонент таблицы
    ├── filters.js              # Переиспользуемый компонент фильтров
    ├── navigation.js           # Компонент навигации и QueryStateManager
    ├── users.js                # Логика страницы пользователей
    ├── stickers.js             # Логика страницы стикерсетов
    ├── generation-logs.js      # Логика страницы логов
    ├── stars-packages.js       # Логика Stars Packages
    ├── art-rules.js            # Логика ART Rules
    ├── prompt-enhancers.js     # Логика Prompt Enhancers
    └── style-presets.js        # Логика Style Presets
```

## 🏗️ Архитектурные принципы

### 1. Модульная структура
- Каждая страница имеет свой HTML и JS файл
- Общие компоненты вынесены в отдельные модули (api.js, utils.js, table.js, filters.js, navigation.js)
- Минимальное дублирование кода

### 2. API Client (api.js)
Централизованный клиент для всех запросов к backend:
- Автоматическое добавление `X-Telegram-Init-Data` заголовка
- Обработка ошибок 401/403 с редиректом на логин
- Универсальный метод `bulkOperation` для массовых операций с partial success
- Улучшенная обработка ошибок с извлечением деталей из response

**Пример использования:**
```javascript
const api = new AdminApiClient();

// Получение данных
const users = await api.getUsers(filters, page, size);

// Массовые операции с отчетом о partial success
await api.bulkOperation(
    selectedIds,
    id => api.blockUser(id),
    'Блокировка пользователей'
);
```

### 3. Переиспользуемые компоненты

#### DataTable (table.js)
Универсальная таблица с:
- Пагинацией
- Сортировкой
- Выбором строк
- Кастомными рендерерами столбцов

#### FiltersPanel (filters.js)
Панель фильтров с поддержкой:
- Текстовых полей
- Select dropdown
- Чекбоксов
- Диапазонов дат
- Дебаунс для текстовых полей

#### Navigation (navigation.js)
- Централизованная конфигурация навигации
- Автоматическая подсветка активной страницы
- QueryStateManager для синхронизации фильтров с URL

### 4. QueryStateManager
Менеджер состояния URL query parameters:
- Синхронизация фильтров с URL
- Поддержка browser history (back/forward)
- Возможность шаринга ссылок с фильтрами
- Подписки на изменения состояния

**Пример использования:**
```javascript
const queryState = createQueryStateManager({
    page: 0,
    size: 20,
    sort: 'createdAt',
    direction: 'DESC'
});

// Получение состояния
const currentState = queryState.getState();

// Обновление состояния
queryState.updateState({ page: 1 });

// Подписка на изменения
queryState.subscribe(newState => {
    console.log('State changed:', newState);
});
```

### 5. Обработка ошибок
- Централизованная обработка на уровне API client
- User-friendly уведомления
- Логирование в консоль для отладки
- Детальные сообщения об ошибках от backend

### 6. Утилиты (utils.js)
Общие функции:
- `formatDate(date)` - форматирование дат
- `formatNumber(num)` - форматирование чисел
- `escapeHtml(text)` - экранирование HTML
- `showNotification(message, type)` - уведомления
- `debounce(func, delay)` - дебаунс для поисковых полей
- `buildQueryString(params)` - построение query string
- `confirmAction(message)` - подтверждение действий

## 🔐 Авторизация

### Telegram Web App авторизация
- Использование `initData` из Telegram Web App
- Хранение в `localStorage`
- Автоматический редирект на `/admin/login.html` при 401/403
- Проверка авторизации через `checkAuth()` на каждой странице

### Права доступа
- Все админ-эндпоинты защищены `@PreAuthorize("hasRole('ADMIN')")`
- Frontend автоматически очищает сессию при 403

## 🎨 UI/UX принципы

### Дизайн
- Tailwind CSS для всех стилей
- Консистентный дизайн-язык
- Адаптивность (responsive design)
- Темная/светлая тема (опционально)

### UX
- Подтверждение деструктивных действий
- Информативные уведомления
- Индикаторы загрузки
- Дружественные сообщения об ошибках
- Поддержка клавиатурной навигации

## 📦 Массовые операции

### Паттерн Bulk Operations
Все массовые операции используют `api.bulkOperation`:
- Выполнение через `Promise.allSettled`
- Partial success - продолжение при ошибках
- Детальный отчет (успешно/ошибки)
- User-friendly уведомления

**Пример:**
```javascript
async function bulkBlock() {
    const selectedIds = table.getSelectedRows();
    if (selectedIds.length === 0) return;
    
    if (!confirm(`Заблокировать ${selectedIds.length} пользователей?`)) {
        return;
    }
    
    await api.bulkOperation(
        selectedIds,
        id => api.blockUser(id),
        'Блокировка пользователей'
    );
    
    table.clearSelection();
    table.refresh();
}
```

## 🔄 Контракты UI↔API

### Соглашения
- RESTful endpoints
- `ResponseEntity<?>` для всех ответов
- Стандартные коды статусов (200, 400, 401, 403, 404, 500)
- JSON payload для ошибок: `{ "error": "...", "message": "..." }`

### Исправленные рассинхроны
- ✅ `DELETE /api/stickersets/{id}/official` для снятия официального статуса
- ✅ Единый паттерн `bulkOperation` для всех массовых операций
- ✅ Консистентная обработка ошибок

## 📊 Реализованные разделы

### Фаза 1: Стабилизация + Финансы
- ✅ Пользователи (Users)
- ✅ Стикерсеты (Stickers)
- ✅ Логи генерации (Generation Logs)
- ✅ Stars Packages
- ✅ ART Rules

### Фаза 2: AI Operations
- ✅ Prompt Enhancers
- ✅ Global Style Presets

### Расширенная модерация
- ✅ Publish/Unpublish стикерсетов
- ✅ Set/Unset Official статус
- ✅ Блокировка/разблокировка
- ✅ Soft delete

## 🚀 Дальнейшее развитие

### Возможные улучшения
1. **Dashboard**: Сводная страница со статистикой
2. **Поиск**: Глобальный поиск по всем разделам
3. **История действий**: Audit log всех админских операций
4. **Права доступа**: Гранулярные роли (SuperAdmin, Moderator, Support)
5. **Экспорт данных**: CSV/JSON экспорт для всех таблиц
6. **Импорт данных**: Массовая загрузка через CSV
7. **Аналитика**: Встроенные графики и метрики
8. **Уведомления**: Real-time уведомления об важных событиях

### Технические улучшения
1. **WebSocket**: Real-time обновления таблиц
2. **Service Worker**: Offline-first подход
3. **Виртуализация**: Для больших таблиц (react-window / virtual-scroller)
4. **TypeScript**: Типизация для лучшей maintainability
5. **Unit tests**: Покрытие тестами критичной логики
6. **E2E tests**: Автоматизированное тестирование сценариев

## 📝 Best Practices

### Код
- ✅ DRY (Don't Repeat Yourself)
- ✅ KISS (Keep It Simple, Stupid)
- ✅ Модульность и переиспользование
- ✅ Консистентный стиль кода
- ✅ Информативные комментарии

### Git
- Осмысленные commit messages
- Атомарные коммиты
- Feature branches для новых фич
- Code review перед мержем

### Безопасность
- ✅ Escape HTML для предотвращения XSS
- ✅ CSRF защита через Spring Security
- ✅ Валидация на backend
- ✅ Минимальные права доступа
