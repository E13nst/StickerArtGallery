# Admin Panel Testing Guide

## Предварительные условия

1. Сервер запущен на `http://localhost:8080`
2. У вас есть валидный `initData` для авторизации
3. Пользователь имеет права администратора

## Тестовый initData

```
user=%7B%22id%22%3A141614461%2C%22first_name%22%3A%22Andrey%22%2C%22last_name%22%3A%22Mitroshin%22%2C%22username%22%3A%22E13nst%22%2C%22language_code%22%3A%22ru%22%2C%22is_premium%22%3Atrue%2C%22allows_write_to_pm%22%3Atrue%2C%22photo_url%22%3A%22https%3A%5C%2F%5C%2Ft.me%5C%2Fi%5C%2Fuserpic%5C%2F320%5C%2Ffnu0NcotQmWRV81w2TFvcn8Zc3Jph2aPIGpxz_gJMWY.svg%22%7D&chat_instance=6208744555757160051&chat_type=private&auth_date=1771323389&signature=NmYyBhkz7lOhtoKLwnC6_oA-rgRGPdkd7zcyHbQApZ_iY2W_NFURmjLw6SR3NtR2gdUf8HY-53_-uZLr5py4Aw&hash=3cc805e835b0654ad7f627775be07fdaee77b0efac91bdbfb8c430bc8908b424
```

## Шаг 1: Авторизация

1. Откройте `http://localhost:8080/admin/login.html`
2. Вставьте initData в текстовое поле
3. Нажмите кнопку "Войти"
4. ✅ Должен произойти редирект на `/admin/index.html`
5. ✅ Не должно быть ошибок в консоли браузера

## Шаг 2: Проверка навигации

После успешной авторизации проверьте боковое меню:

- ✅ Пользователи
- ✅ Стикерсеты
- ✅ Логи генерации
- ✅ ⭐ Stars Packages
- ✅ 🎭 ART Rules
- ✅ ✨ Prompt Enhancers
- ✅ 🎨 Style Presets

Все новые пункты меню должны быть видны и кликабельны.

## Шаг 3: Stars Packages (`/admin/stars-packages.html`)

### Проверка загрузки данных

1. Откройте страницу
2. ✅ Индикатор загрузки должен появиться и исчезнуть
3. ✅ Если данных нет: показывается "Тарифы не найдены"
4. ✅ Если данные есть: таблица заполнена

### Проверка консоли браузера

Откройте DevTools (F12) → Console:
- ✅ Не должно быть красных ошибок
- ✅ Запрос к `/api/admin/stars-packages` должен вернуть 200 OK

### Проверка функциональности

1. Нажмите "Добавить тариф"
   - ✅ Модальное окно открывается
   - ✅ Все поля формы доступны
   
2. Заполните форму:
   - Название: "Тестовый тариф"
   - ART токены: 100
   - Стоимость (Stars): 50
   - Бонус: 10
   - Порядок: 0
   - Активен: ✓
   
3. Нажмите "Сохранить"
   - ✅ Появляется уведомление об успехе
   - ✅ Модальное окно закрывается
   - ✅ Таблица обновляется с новым тарифом

4. Проверьте кнопки действий:
   - ✅ "Изменить" открывает форму редактирования
   - ✅ "Деактивировать/Активировать" меняет статус
   - ✅ "Удалить" запрашивает подтверждение и удаляет

### Ожидаемые API endpoints

```
GET    /api/admin/stars-packages          → Список тарифов
POST   /api/admin/stars-packages          → Создание тарифа
PUT    /api/admin/stars-packages/{id}     → Обновление тарифа
PATCH  /api/admin/stars-packages/{id}/toggle → Переключение статуса
DELETE /api/admin/stars-packages/{id}     → Удаление тарифа
```

## Шаг 4: ART Rules (`/admin/art-rules.html`)

### Проверка загрузки данных

1. Откройте страницу
2. ✅ Индикатор загрузки должен появиться и исчезнуть
3. ✅ Если данных нет: показывается "Правила не найдены"
4. ✅ Если данные есть: таблица заполнена

### Проверка консоли браузера

- ✅ Не должно быть красных ошибок
- ✅ Запрос к `/api/admin/art-rules` должен вернуть 200 OK

### Проверка функциональности

1. Нажмите "Добавить правило"
   - ✅ Модальное окно открывается
   
2. Заполните форму:
   - AI модель: FLUX_1_1_PRO
   - Тип контента: STICKER
   - Базовая стоимость: 10
   - Доп. множитель: 1.0
   - Порядок: 0
   - Активно: ✓
   
3. Нажмите "Сохранить"
   - ✅ Появляется уведомление об успехе
   - ✅ Правило добавляется в таблицу

4. Проверьте отображение:
   - ✅ Название модели форматируется корректно (FLUX 1.1 Pro)
   - ✅ Тип контента показывается как badge (Стикер)
   - ✅ Статус отображается цветным badge

### Ожидаемые API endpoints

```
GET    /api/admin/art-rules          → Список правил
POST   /api/admin/art-rules          → Создание правила
PUT    /api/admin/art-rules/{id}     → Обновление правила
PATCH  /api/admin/art-rules/{id}/toggle → Переключение статуса
DELETE /api/admin/art-rules/{id}     → Удаление правила
```

## Шаг 5: Prompt Enhancers (`/admin/prompt-enhancers.html`)

### Проверка загрузки данных

1. Откройте страницу
2. ✅ Индикатор загрузки должен появиться и исчезнуть
3. ✅ Если данных нет: показывается "Энхансеры не найдены"
4. ✅ Если данные есть: таблица заполнена

### Проверка консоли браузера

- ✅ Не должно быть красных ошибок
- ✅ Запрос к `/api/admin/prompt-enhancers` должен вернуть 200 OK

### Проверка функциональности

1. Нажмите "Добавить энхансер"
   - ✅ Модальное окно открывается
   
2. Заполните форму:
   - Название: "Стандартный энхансер"
   - Описание: "Улучшает промпты для стикеров"
   - Шаблон: "Create a cute sticker of {USER_PROMPT}, high quality, detailed"
   - Приоритет: 0
   - Активен: ✓
   
3. Нажмите "Сохранить"
   - ✅ Появляется уведомление об успехе
   - ✅ Энхансер добавляется в таблицу

4. Проверьте валидацию:
   - Попробуйте сохранить без `{USER_PROMPT}` в шаблоне
   - ✅ Должна появиться ошибка: "Шаблон должен содержать плейсхолдер {USER_PROMPT}"

### Ожидаемые API endpoints

```
GET    /api/admin/prompt-enhancers          → Список энхансеров
POST   /api/admin/prompt-enhancers          → Создание энхансера
PUT    /api/admin/prompt-enhancers/{id}     → Обновление энхансера
PATCH  /api/admin/prompt-enhancers/{id}/toggle → Переключение статуса
DELETE /api/admin/prompt-enhancers/{id}     → Удаление энхансера
```

## Шаг 6: Style Presets (`/admin/style-presets.html`)

### Проверка загрузки данных

1. Откройте страницу
2. ✅ Индикатор загрузки должен появиться и исчезнуть
3. ✅ Если данных нет: показывается "Пресеты не найдены"
4. ✅ Если данные есть: таблица заполнена

### Проверка консоли браузера

- ✅ Не должно быть красных ошибок
- ✅ Запрос к `/api/admin/style-presets/global` должен вернуть 200 OK

### Проверка функциональности

1. Нажмите "Добавить пресет"
   - ✅ Модальное окно открывается
   
2. Заполните форму:
   - Название: "Минимализм"
   - Описание: "Минималистичный стиль"
   - Стиль промпта: "minimalist, clean lines, simple shapes, flat design"
   - URL миниатюры: (оставить пустым или указать URL)
   - Порядок: 0
   - Активен: ✓
   
3. Нажмите "Сохранить"
   - ✅ Появляется уведомление об успехе
   - ✅ Пресет добавляется в таблицу

4. Проверьте отображение миниатюры:
   - Если URL указан: ✅ Показывается изображение 12x12
   - Если URL не указан: ✅ Показывается "—"

### Ожидаемые API endpoints

```
GET    /api/admin/style-presets/global          → Список пресетов
POST   /api/admin/style-presets/global          → Создание пресета
PUT    /api/admin/style-presets/global/{id}     → Обновление пресета
PATCH  /api/admin/style-presets/global/{id}/toggle → Переключение статуса
DELETE /api/admin/style-presets/global/{id}     → Удаление пресета
```

## Автоматическая проверка через консоль браузера

Вставьте этот код в консоль браузера (F12 → Console) после авторизации:

```javascript
// Проверка всех API endpoints
async function testAllEndpoints() {
    const results = {
        starsPackages: { tested: false, success: false, error: null },
        artRules: { tested: false, success: false, error: null },
        promptEnhancers: { tested: false, success: false, error: null },
        stylePresets: { tested: false, success: false, error: null }
    };
    
    // Test Stars Packages
    try {
        const packages = await api.getStarsPackages();
        results.starsPackages.tested = true;
        results.starsPackages.success = Array.isArray(packages);
        console.log('✅ Stars Packages:', packages);
    } catch (error) {
        results.starsPackages.tested = true;
        results.starsPackages.error = error.message;
        console.error('❌ Stars Packages error:', error);
    }
    
    // Test ART Rules
    try {
        const rules = await api.getArtRules();
        results.artRules.tested = true;
        results.artRules.success = Array.isArray(rules);
        console.log('✅ ART Rules:', rules);
    } catch (error) {
        results.artRules.tested = true;
        results.artRules.error = error.message;
        console.error('❌ ART Rules error:', error);
    }
    
    // Test Prompt Enhancers
    try {
        const enhancers = await api.getPromptEnhancers();
        results.promptEnhancers.tested = true;
        results.promptEnhancers.success = Array.isArray(enhancers);
        console.log('✅ Prompt Enhancers:', enhancers);
    } catch (error) {
        results.promptEnhancers.tested = true;
        results.promptEnhancers.error = error.message;
        console.error('❌ Prompt Enhancers error:', error);
    }
    
    // Test Style Presets
    try {
        const presets = await api.getGlobalStylePresets();
        results.stylePresets.tested = true;
        results.stylePresets.success = Array.isArray(presets);
        console.log('✅ Style Presets:', presets);
    } catch (error) {
        results.stylePresets.tested = true;
        results.stylePresets.error = error.message;
        console.error('❌ Style Presets error:', error);
    }
    
    // Summary
    console.log('\n=== TEST SUMMARY ===');
    console.table(results);
    
    const allSuccess = Object.values(results).every(r => r.success);
    if (allSuccess) {
        console.log('✅ ВСЕ ТЕСТЫ ПРОШЛИ УСПЕШНО!');
    } else {
        console.log('❌ НЕКОТОРЫЕ ТЕСТЫ НЕ ПРОШЛИ');
    }
    
    return results;
}

// Запустить тесты
testAllEndpoints();
```

## Возможные проблемы и решения

### Проблема: 401 Unauthorized

**Причина:** initData невалиден или истек  
**Решение:** Перелогиньтесь с актуальным initData

### Проблема: 403 Forbidden

**Причина:** Пользователь не является администратором  
**Решение:** Убедитесь, что у пользователя есть роль ADMIN в базе данных

### Проблема: 404 Not Found

**Причина:** Backend endpoint не реализован  
**Решение:** Проверьте, что соответствующий контроллер создан и маппинг корректен

### Проблема: CORS ошибка

**Причина:** Неправильная конфигурация CORS  
**Решение:** Проверьте настройки CORS в Spring Boot

### Проблема: Данные не загружаются

**Причина:** Ошибка в JavaScript или API  
**Решение:** 
1. Откройте DevTools → Network
2. Проверьте статус запросов
3. Посмотрите ответ сервера
4. Проверьте консоль на ошибки JavaScript

## Чеклист финальной проверки

- [ ] Авторизация работает
- [ ] Все пункты меню видны
- [ ] Stars Packages: загрузка, создание, редактирование, удаление
- [ ] ART Rules: загрузка, создание, редактирование, удаление
- [ ] Prompt Enhancers: загрузка, создание, редактирование, удаление
- [ ] Style Presets: загрузка, создание, редактирование, удаление
- [ ] Нет ошибок в консоли браузера
- [ ] Все уведомления работают корректно
- [ ] Модальные окна открываются и закрываются
- [ ] Кнопка "Выйти" работает на всех страницах

## Отчет о тестировании

После прохождения всех тестов заполните:

```
Дата тестирования: __________
Тестировщик: __________

Stars Packages:
- Загрузка данных: ✅/❌
- Создание: ✅/❌
- Редактирование: ✅/❌
- Удаление: ✅/❌
- Ошибки: __________

ART Rules:
- Загрузка данных: ✅/❌
- Создание: ✅/❌
- Редактирование: ✅/❌
- Удаление: ✅/❌
- Ошибки: __________

Prompt Enhancers:
- Загрузка данных: ✅/❌
- Создание: ✅/❌
- Редактирование: ✅/❌
- Удаление: ✅/❌
- Ошибки: __________

Style Presets:
- Загрузка данных: ✅/❌
- Создание: ✅/❌
- Редактирование: ✅/❌
- Удаление: ✅/❌
- Ошибки: __________

Общие замечания:
__________
```
