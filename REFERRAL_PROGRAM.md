# Реферальная программа

## Описание

Реферальная программа предоставляет механизм для привлечения новых пользователей через систему вознаграждений.

### Правила начисления

1. **Приглашённому (invitee)**: +100 ART сразу при первой аутентификации по реферальной ссылке
2. **Пригласившему (referrer)**: +50 ART после первой генерации стикера приглашённым

## Архитектура

### База данных

#### Таблица `referral_codes`
Хранит уникальные реферальные коды пользователей.

- `user_id` (PK) - Telegram ID владельца кода
- `code` (UNIQUE) - Уникальный код (base62, 12 символов)
- `created_at` - Время создания

#### Таблица `referrals`
Хранит связи между пригласившими и приглашёнными пользователями.

- `id` (PK)
- `referrer_user_id` - Telegram ID реферера
- `referred_user_id` (UNIQUE) - Telegram ID приглашённого (может быть только у одного реферера)
- `status` - Статус связи (ACTIVE/INACTIVE)
- `start_param` - Исходный параметр из initData
- `metadata` (JSONB) - Метаданные для антифрода
- `invitee_bonus_awarded_at` - Время начисления бонуса приглашённому
- `referrer_first_generation_awarded_at` - Время начисления бонуса рефереру

**Constraints:**
- `CHECK (referrer_user_id != referred_user_id)` - защита от self-referral
- `UNIQUE (referred_user_id)` - один реферер на пользователя

#### Таблица `referral_events`
Аудит событий реферальной программы.

- `id` (PK)
- `referral_id` - FK на referrals
- `event_type` - Тип события (INVITEE_BONUS_GRANTED, FIRST_GENERATION_REWARD_GRANTED)
- `art_transaction_id` - FK на связанную ART транзакцию
- `external_id` (UNIQUE) - Внешний ID для идемпотентности
- `created_at` - Время события

### API

#### `GET /api/referrals/me/link`

Получить реферальную ссылку текущего пользователя.

**Response:**
```json
{
  "code": "AbC123XyZ456",
  "startParam": "ref_AbC123XyZ456",
  "url": "https://t.me/stixlybot?startapp=ref_AbC123XyZ456"
}
```

### Конфигурация

#### Переменные окружения

```bash
# Дневной лимит начислений рефереру (по умолчанию 50)
REFERRAL_DAILY_CAP=50

# Окно атрибуции в днях (по умолчанию 7)
REFERRAL_ATTRIBUTION_WINDOW_DAYS=7

# Длина реферального кода (по умолчанию 12)
REFERRAL_CODE_LENGTH=12
```

#### application.yaml

```yaml
app:
  referral:
    daily-referrer-reward-cap: ${REFERRAL_DAILY_CAP:50}
    attribution-window-days: ${REFERRAL_ATTRIBUTION_WINDOW_DAYS:7}
    code-length: ${REFERRAL_CODE_LENGTH:12}
```

## Механизм работы

### 1. Генерация реферальной ссылки

Пользователь запрашивает свою реферальную ссылку через `GET /api/referrals/me/link`.

- Если код не существует, генерируется уникальный base62 код
- Формируется deep-link: `https://t.me/<bot_username>?startapp=ref_<code>`

### 2. Переход по реферальной ссылке

Новый пользователь открывает Mini App через реферальную ссылку.

- Telegram передаёт `start_param` в `initData` при первом запросе
- `TelegramAuthenticationProvider` извлекает `start_param`
- `ReferralService.onFirstAuthentication()` обрабатывает атрибуцию

### 3. Атрибуция и начисление бонуса приглашённому

При первой аутентификации с `start_param=ref_*`:

1. Валидация формата `start_param`
2. Проверка, не атрибутирован ли уже пользователь
3. Поиск реферера по коду
4. Self-referral защита
5. Создание записи в `referrals`
6. Начисление +100 ART приглашённому через `ArtRewardService.award()`
7. Создание события `INVITEE_BONUS_GRANTED` в `referral_events`

**Идемпотентность:**
- `externalId = "referral:invitee_bonus:" + referredUserId`
- Повторные вызовы не создают дубликатов благодаря UNIQUE constraint

### 4. Начисление бонуса рефереру

При первой генерации приглашённого пользователя:

1. `StickerGenerationService.startGeneration()` вызывает `ReferralService.onFirstGeneration()`
2. Проверка наличия реферальной связи
3. Проверка, не начислена ли уже награда
4. **Дневной cap**: не более N начислений за 24ч
5. **Окно атрибуции**: первая генерация должна быть в течение N дней после регистрации
6. Начисление +50 ART рефереру через `ArtRewardService.award()`
7. Обновление `referrer_first_generation_awarded_at`
8. Создание события `FIRST_GENERATION_REWARD_GRANTED` в `referral_events`

**Идемпотентность:**
- `externalId = "referral:first_generation:" + referredUserId`
- Повторные генерации не начисляют бонус повторно

## Антифрод меры

### 1. Self-referral защита
Пользователь не может пригласить сам себя:
```sql
CHECK (referrer_user_id != referred_user_id)
```

### 2. Один реферер на пользователя
Пользователь не может быть переатрибутирован:
```sql
UNIQUE (referred_user_id)
```

### 3. Дневной cap для рефереров
Ограничение количества начислений одному рефереру за 24 часа (по умолчанию 50).

### 4. Окно атрибуции
Бонус рефереру начисляется только если первая генерация произошла в течение N дней после регистрации (по умолчанию 7).

### 5. Метаданные для fingerprinting
В `referrals.metadata` (JSONB) сохраняются данные для будущего анализа:
- `source` - источник регистрации
- `user_agent_hash` - хеш user agent (опционально)
- `ip_hash` - хеш IP адреса (опционально)

## ART Rules

В таблице `art_rules` созданы два правила:

```sql
INSERT INTO art_rules (code, direction, amount, is_enabled, description)
VALUES 
  ('REFERRAL_INVITEE_BONUS', 'CREDIT', 100, TRUE, 'Бонус приглашённому пользователю за регистрацию по реферальной ссылке'),
  ('REFERRAL_REFERRER_FIRST_GENERATION', 'CREDIT', 50, TRUE, 'Бонус рефереру за первую генерацию стикера приглашённым пользователем');
```

## Расширяемость

Архитектура позволяет легко добавить дополнительные события:

1. **Telegram Stars**: добавить событие `FIRST_STARS_PURCHASE_REWARD_GRANTED`
2. **Многоуровневая реферальная программа**: добавить поле `parent_referral_id`
3. **Динамические награды**: использовать `art_rules.amount` вместо хардкода

## Тестирование

Тесты находятся в `ReferralServiceTest.java`:

- ✅ Создание уникального реферального кода
- ✅ Идемпотентность генерации кода
- ✅ Создание реферальной связи и начисление бонуса приглашённому
- ✅ Защита от дубликатов
- ✅ Self-referral защита
- ✅ Начисление бонуса рефереру за первую генерацию
- ✅ Защита от повторного начисления рефереру

## Логирование

Все критические операции логируются с эмодзи для удобства поиска:

- 🔗 - генерация реферальной ссылки
- 🎁 - обработка реферальной атрибуции
- 🎨 - проверка бонуса за первую генерацию
- ✅ - успешные операции
- ⚠️ - предупреждения (cap, окно атрибуции, конфликты)
- ❌ - ошибки

## Примеры запросов

### Получить свою реферальную ссылку

```bash
curl -X GET "https://api.example.com/api/referrals/me/link" \
  -H "Authorization: tma initDataRaw"
```

**Response:**
```json
{
  "code": "AbC123XyZ456",
  "startParam": "ref_AbC123XyZ456",
  "url": "https://t.me/stixlybot?startapp=ref_AbC123XyZ456"
}
```

## Миграция

Flyway миграция: `V1_0_47__Create_referrals.sql`

Создаёт все необходимые таблицы, индексы, constraints и добавляет правила в `art_rules`.
